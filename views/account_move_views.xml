<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- VIEWS -->
        <record id="view_move_form_inherit_hair_by_ning" model="ir.ui.view">
            <field name="name">account.move.form.inherit.hair_by_ning</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/list" position="attributes">
                <attribute name="editable"/> <!-- to unset the original value of 'bottom' -->
            </xpath>
            </field>
        </record>

    </data>
</odoo>
