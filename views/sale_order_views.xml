<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- VIEWS -->
    <record id="view_order_form_hair_by_ning_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.hair_by_ning</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='order_lines']/field[@name='order_line']/list" position="attributes">
                <attribute name="editable"/> 
            </xpath>
        </field>
    </record>
</odoo>
